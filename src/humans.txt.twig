
/* TEAM */
    Artist: {{ artist.name }}
    Site: {{ artist.url }}
    {% if artist.twitter %}Twitter: @{{ artist.twitter }}{% endif %}
    Location: {{ artist.location }}

/* SITE */
    Standards: HTML, CSS, OGP
    Preprocessing: Gulp, Twig
